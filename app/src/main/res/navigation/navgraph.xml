<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
             xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/navgraph"
    app:startDestination="@id/tanishuvFragment">

    <fragment
        android:id="@+id/tanishuvFragment"
        android:name="www.uzmd.yuklovchi.presentation.TanishuvFragment"
        android:label="fragment_tanishuv"
        tools:layout="@layout/fragment_tanishuv" >
        <action
            android:id="@+id/action_tanishuvFragment_to_mainFragment"
            app:destination="@id/mainFragment"
            app:launchSingleTop="false"
            app:popUpToInclusive="true" />
    </fragment>
    <fragment
        android:id="@+id/mainFragment"
        android:name="www.uzmd.yuklovchi.presentation.MainFragment"
        android:label="fragment_main"
        tools:layout="@layout/fragment_main" />
</navigation>